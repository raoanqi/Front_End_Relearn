/**
 * 可以把对象分为几类：
 *
 * 宿主对象：由js的宿主对象提供，它们的行为完全由宿主环境决定
 * 内置的对象：有js语言提供的对象
 * 固有对象：由标准规定，随着js运行时创建而自动创建的对象实例
 * 原生对象：可以由用户通过Array，RegExp等内置构造器或者特殊的语法创建的对象
 * 普通对象：有{}，Object构造器或者class关键字定义类创建的对象，能够被原型继承
 *
 *
 *
 */